name: test
on:
  push:
    branches:
      - "**"

jobs:
  echo1:
    runs-on: ubuntu-latest
    steps:
      - uses: whywaita/setup-lxd@v1
      - uses: actions/checkout@v2
      - run: |
          sudo rm -rf /opt/hostedtoolcache/* /opt/ghc
      - uses: mxschmitt/action-tmate@v3
        if: ${{ always() }}
