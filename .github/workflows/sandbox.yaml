name: test
on:
  push:
    branches:
      - "**"
  workflow_dispatch:

jobs:
  echo1:
    runs-on: macos-latest
    steps:
     - uses: actions/checkout@v4
     - run: ls -la
     #     - run: ls -la /opt/hostedtoolcache
     - run: echo ${USER} ; id
     - run: ls -la ${HOME}/.local
     - uses: whywaita/is-installed-xcode-action@98a80e63d1d95fb3cb5fab03424dd16d867b4f54
     - name: Run tmate
       uses: mxschmitt/action-tmate@v3
       if: always()
